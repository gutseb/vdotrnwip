---
- name: debug out organization
  debug:
    var: organization
    verbosity: 1

- name: "ORG: {{ organization.id }} {{ organization.name }} : Create directory for organization {{ organization.name }} artifacts"
  file:
    path: '{{ git_root_dir.path }}/repo/managed/organizations/{{ organization.id }}'
    state: directory

- name: "ORG: {{ organization.id }} {{ organization.name }} : Create organization configuration."
  copy: 
    content: "{{ organization | to_nice_yaml }}" 
    dest: '{{ git_root_dir.path }}/repo/managed/organizations/{{ organization.id }}.yaml'

- name: Artifact Type Loop
  include_tasks: artifact_block.yml
  loop:
    # - name: products
    #   is_katello: true
    - name: locations
      is_katello: false
    - name: activation_keys
      is_katello: true
  loop_control:
    loop_var: artifact_type
        